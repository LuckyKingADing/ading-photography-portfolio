---
import BaseLayout from '../components/BaseLayout.astro';
import { site } from '../data/site';
---

<BaseLayout
  title="关于"
  description="关于 ADING，以及照片背后的观察方式。"
  current="about"
>
  <section class="about-intro">
    <p class="eyebrow">关于 / ADING</p>
    <h1>在姿态成形之前，看见真实的瞬间。</h1>
  </section>

  <section class="about-grid">
    <div class="about-index" aria-hidden="true">A</div>
    <div class="about-copy">
      <p class="lead">ADING 围绕人像、地点与安静观察，持续建立个人摄影实践。</p>
      <p>创作从自然光与专注开始：一个人如何存在于房间里，街道如何在雨后改变，或一个细小动作如何容纳完整记忆。</p>
      <p>这个作品集会以一个个摄影系列逐渐生长。每组选片保持克制，让每一张画面都真正参与叙事。</p>
    </div>
    <aside class="contact-block" aria-labelledby="contact-title">
      <p class="eyebrow" id="contact-title">联系</p>
      {site.email || site.instagramUrl ? (
        <ul>
          {site.email && <li><a href={`mailto:${site.email}`}>{site.email}</a></li>}
          {site.instagramUrl && <li><a href={site.instagramUrl} rel="me">Instagram</a></li>}
        </ul>
      ) : (
        <p>正式发布第一组摄影作品前，这里会补充联系信息。</p>
      )}
    </aside>
  </section>
</BaseLayout>
