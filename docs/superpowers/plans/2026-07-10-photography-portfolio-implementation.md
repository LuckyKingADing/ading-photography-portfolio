# ADING Photography Portfolio — Implementation Plan

1. Scaffold the minimal Astro project, semantic tokens, shared layout, and base-path helper. Verify with a production build.
2. Add typed series content plus local abstract placeholders and render the home page. Verify data-driven routes and responsive layout.
3. Build the series gallery and progressively enhanced native-dialog lightbox. Verify mouse, keyboard, Escape, and no-JavaScript fallback behavior.
4. Add About and 404 pages, editable contact settings, metadata, favicon, and image replacement guide. Verify internal links and page titles.
5. Add GitHub Pages deployment workflow and repository configuration. Verify the local build with the production base path.
6. Run browser checks at desktop and mobile widths, audit focus and reduced-motion behavior, and correct visual defects.
7. Commit the finished site, create the public GitHub repository, push `main`, enable GitHub Actions Pages, and verify the live URL.
